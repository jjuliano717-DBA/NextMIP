# Next MIP Website

This project is a static website for Next MIP, built with HTML, Tailwind CSS, and JavaScript.

## Setup

No build step is required as Tailwind CSS is loaded via CDN.

To verify the website locally, simply open `index.html` in your web browser.

## File Structure

- `index.html`: The main landing page containing all structure, styles, and scripts.
